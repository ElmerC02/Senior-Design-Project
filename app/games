export default function GamesPage() {
  return (
    <section>
      <h1 className="header">Games</h1>
      <div className="grid">
        {["2048", "Snake", "Tiles"].map((g) => (
          <article className="card" key={g}>
            <h3>{g}</h3>
            <p>Playable game embedding to be integrated.</p>
            <a href="#">Play</a>
          </article>
        ))}
      </div>
    </section>
  );
}
